import { Component, inject, OnInit, signal } from '@angular/core'; import { ActivatedRoute, Router } from '@angular/router'; import { Location } from '@angular/common'; import { ToastrService } from 'ngx-toastr'; import { SpeakerDetailsModel } from '../../../../../core/models/speaker/speaker.model'; import { SpeakerService } from '../../../../../core/services/speaker/speaker.service';
@Component({selector:'app-speaker-details',standalone:true,templateUrl:'./speaker-details.html',styleUrl:'./speaker-details.css'}) export class SpeakerDetails implements OnInit{private service=inject(SpeakerService);private route=inject(ActivatedRoute);private router=inject(Router);private location=inject(Location);private toastr=inject(ToastrService);speaker=signal<SpeakerDetailsModel|null>(null);loading=signal(true);eventId='';ngOnInit(){this.eventId=this.route.pathFromRoot.map(r=>r.snapshot.paramMap.get('eventId')).find(Boolean)??'';const id=this.route.snapshot.paramMap.get('speakerId')??'';this.service.getSpeakerById(this.eventId,id).subscribe({next:r=>{this.speaker.set(r.data??null);this.loading.set(false);},error:e=>{console.error(e);this.loading.set(false);this.toastr.error('Failed to load speaker.');}})}edit(){const id=this.speaker()?.id;if(id)this.router.navigate(['../',id,'edit'],{relativeTo:this.route});}back(){this.location.back()}}
