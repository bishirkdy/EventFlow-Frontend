import { Component, inject, OnInit, signal } from '@angular/core'; import { ActivatedRoute, Router } from '@angular/router'; import { Location } from '@angular/common'; import { ToastrService } from 'ngx-toastr'; import { SponsorModel } from '../../../../../core/models/sponsor/sponsor.model'; import { SponsorService } from '../../../../../core/services/sponsor/sponsor.service';
@Component({selector:'app-sponsor-details',standalone:true,templateUrl:'./sponsor-details.html',styleUrl:'./sponsor-details.css'}) export class SponsorDetails implements OnInit{private service=inject(SponsorService);private route=inject(ActivatedRoute);private router=inject(Router);private location=inject(Location);private toastr=inject(ToastrService);sponsor=signal<SponsorModel|null>(null);loading=signal(true);eventId='';ngOnInit(){this.eventId=this.route.pathFromRoot.map(r=>r.snapshot.paramMap.get('eventId')).find(Boolean)??'';const id=this.route.snapshot.paramMap.get('sponsorId')??'';this.service.getSponsorById(this.eventId,id).subscribe({next:r=>{this.sponsor.set(r.data??null);this.loading.set(false)},error:e=>{console.error(e);this.loading.set(false);this.toastr.error('Failed to load sponsor.')}})}edit(){const id=this.sponsor()?.id;if(id)this.router.navigate(['../',id,'edit'],{relativeTo:this.route})}back(){this.location.back()}}
